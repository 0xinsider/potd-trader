@AGENTS.md

# Claude Code

`AGENTS.md` is the contract. Follow its "Setting it up for a person" steps in order and stop
where it says stop. The two lines that matter most: never print or ask for a key, and never set
`LIVE=yes` yourself.
